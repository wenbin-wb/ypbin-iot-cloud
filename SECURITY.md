# 安全政策（Security Policy）

感谢你帮助让 `ypbin-iot-cloud` 更安全。

## 支持范围

本项目**尚未发布任何版本**（开发版 `0.1.0-SNAPSHOT`，Maven Central 上无此坐标），
因此**没有「受支持的发布版本」**。当前按以下方式处理：

| 版本 | 是否提供安全修复 |
|---|---|
| `master`（当前唯一分支） | ✅ 尽力修复 |
| 已发布的正式版 | 暂无（尚未发布任何版本） |

> 本仓当前状态是 **M0a 骨架**：只有三个可启动的部署单元与门禁，**没有任何业务逻辑**，
> 也**没有**认证、鉴权、入站可信校验与设备接入实现。请按「可以读、可以编译、不要部署到生产」看待它。

## 如何上报漏洞

**请不要通过公开 Issue 上报安全问题**，以免在修复前被利用。请任选一种私密渠道：

1. 仓库托管后的 GitHub 私密漏洞上报：仓库 **Security** 标签页 → *Report a vulnerability*
   （推荐，可在线协作修复）；
2. 邮件：发送至仓库公开资料中的维护者邮箱，标题以 `[SECURITY] ypbin-iot-cloud` 开头。

上报时请尽量包含：

- 受影响的模块与提交/版本（例如 `ypbin-iot-cloud-business` @ `abc1234`）；
- 复现步骤或最小可复现样例（含 JDK 版本、启动参数、相关配置）；
- 影响面判断（能读到什么、能改成什么、是否需要网络位置或凭据）；
- 若已知，附上缓解建议。

## 我们的处理节奏

- **48 小时内**确认收到并给出初步判断；
- 确认有效后，在修复合并前与你同步进展（如需，可约定协调披露窗口）；
- 修复合并后致谢（如你愿意具名）。

## 已知的安全边界与实现缺口（避免误报）

以下都是**当前阶段尚未实现的计划内事项**，不是漏洞；但请勿据此认为系统可对抗攻击：

- **入站可信校验尚未实现**：网关剥离伪造头、下游入站守卫（fail-closed / 常量时间比较 /
  失败转 HTTP 200 + `R.code=401`）属 P1/P2 交付（设计见 `IOT-CLOUD-SPEC.md` §4.4）。
- **无认证与鉴权**：M0a 的三个部署单元只有启动类，没有登录、令牌与权限模型。
- **无数据库与中间件**：M0a 不接 MySQL / Redis / EMQX / IoTDB，也没有 Flyway 迁移。
- **`access` 尚未接设备接入框架**（`ypbin-iot-starter`，P4）——
  当前不存在任何南向协议（Modbus / OPC UA / MQTT / TCP）的实现代码。
- **本地端口 18080/18081/18082 是 M0a 占位值**，仅用于本地探活；
  任何部署形态都要求「下游端口不对不可信网络暴露」——入站校验只是纵深防御，不是唯一防线。

## 供应链与静态扫描

- CI 对 `master` 每个提交执行**第一批门禁**：架构约束（ArchUnit + 规则有效性自检）、源码规范、
  模块发布边界、spotless、覆盖率门禁；
- CodeQL（`security-and-quality`）在 push / PR / 每周定时运行，告警可在 Security 面板查看；
- 依赖更新由 Dependabot 按同族分组提 PR（见 `.github/dependabot.yml`）；
- **尚未接入**（M0b 起）：NullAway 空值语义、依赖版本收敛、SBOM、容器集成测试。
  相关脚本在 `tools/` 中已就位并在 `tools/preflight.sh` 里登记。
